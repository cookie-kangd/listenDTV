# listenDTV v0.1.16 更新说明

## 🆕 模拟器 / 跨网段同步支持（拉取同步 + 手动输入 IP + 我的二维码）

局域网自动搜索（mDNS + UDP 广播 + TCP 扫描）**原样保留**，负责同一 WiFi 下真机之间的互搜。本版新增三条路径，覆盖自动搜索**原理上无法覆盖**的场景——最典型的是：**另一台"设备"其实是 Windows 上的安卓模拟器**。

### 为什么模拟器搜不到（不是 bug）

模拟器默认走 **NAT 网络**：它拿到的是虚拟网段 IP（如 `10.0.2.x`），与手机的 `192.168.x.x` 不同网段，广播/组播都跨不过 NAT——任何局域网发现方案（包括微信、投屏软件）在这种拓扑下都会失败。

**解决方式（任选其一）：**

1. **模拟器开"网络桥接"模式**（推荐）：MuMu 12 在 设置中心→网络设置→网络桥接；雷电 9 在 软件设置→网络设置→桥接模式（需管理员权限重启模拟器）。桥接后模拟器拿到真实局域网 IP，自动搜索、双向同步全部直通。注意：PC 用**网线**上网桥接才稳，WiFi 上网多数网卡不支持。
2. **不开桥接也能同步**（本次新增的功能，见下）。

### 本次新增功能

- **拉取模式（同步方向第 4 个图标）**：同步弹窗右上角的模式图标循环切换，新增"拉取"档。选中后点击对方设备 = 把**对方**的历史/收藏拉到**本机**。拉取只要求"本机能连上对方"，不要求对方能回连——**NAT 模拟器场景的双向同步靠它实现**：模拟器拉手机（手机历史→模拟器），手机推模拟器或模拟器推手机（模拟器历史→手机）。
- **手动输入 IP**：同步弹窗新增输入框，直接输入对方 IP（如 `192.168.1.7`，自动补全端口 9978），立即探测并给出"连接成功/无响应"明确反馈。模拟器没有摄像头扫不了二维码，用它最方便。
- **我的二维码**：同步弹窗新增二维码按钮，展示本机地址二维码；另一台手机用弹窗里原有的扫码按钮扫一下即可添加设备（模拟器桥接后也可被扫）。

### 使用建议（手机 + NAT 模拟器）

1. 两台都升级到 0.1.16。
2. 模拟器：同步弹窗 → 手动输入手机的 IP（手机在 弹窗右上角二维码里也能看到自己的地址）。
3. 模拟器历史 → 手机：模拟器选"上传/双向"模式点手机设备。
4. 手机历史 → 模拟器：模拟器选"拉取"模式点手机设备。

### 其他

- 同步弹窗所有对端请求改用直连客户端（与 0.1.14 扫描修复同一原理，避免被代理/DoH 劫持局域网请求）。
- 服务端新增 `/action?do=export` 导出端点，供拉取使用；原有同步/投屏行为零改动。
- 依赖审计：已是最新稳定版，无升级项。
- versionCode 580，versionName 0.1.16。
