# listenDTV v0.1.12 更新内容

## 🔊 修复听播模式开关失灵（重点）
- 现象：进入视频默认听播开启，但点击耳机图标后图标仍亮着、听播却已关闭并出现画面；此后无论点多少次都显示"听播模式已关闭"，再也进不去听播
- 原因：v0.1.10 引入的「按视频记忆听播」在读取偏好设置时，把空字符串直接交给了 JSON 解析器。空字符串不是合法 JSON，解析会抛异常并被静默忽略——结果是**读**永远拿默认值（开启）、**写**一次都没真正保存进去，所以每次点击都在重复"关闭"动作
- 修复：
  - 空值/异常一律当作"还没有记录"处理，读取与写入都走安全的解析路径，保存真正生效
  - 点击后回读真实保存结果再同步播放器、图标和提示，三者再也不会出现"图标亮着但实际关闭"的错位
  - 按视频记忆听播的行为保持不变：每部视频独立记忆，未设置过的视频默认开启

## 🧹 回归排查与清理
- 逐文件比对了 v0.1.6 以来所有改动过的地方，结论如下：
  - 播放器核心（PlayerManager）与 v0.1.6 逐字节一致，v0.1.9 的回退干净无残留
  - 直播页、BaseActivity 等未被改动过
  - 退出清理缓存、自动检查更新开关、竖屏控制栏按钮尺寸等改动均正常，无关联损坏
  - 清理掉一处无效代码：首页底部毛玻璃导航的低版本回退分支（本项目最低支持 Android 7.0，该分支永远走不到，不影响任何显示效果）
- 听播模式原「全局开关」的旧偏好值已不再参与判断（现在按视频记忆、默认开启），这是 v0.1.10 按视频需求的有意设计，非异常

## ⚡ 性能与依赖
- 听播状态读写不再靠抛异常兜底，避免了每次点击/切后台都构造异常对象的额外开销
- 构建已开启代码压缩与资源缩减，运行时未发现新的、可安全改动的性能瓶颈
- 依赖核查：appcompat / recyclerview / room / lifecycle / material / okhttp / glide / lottie / eventbus 等均为当前最新稳定版，media3 为源码构建不通过版本号升级，**无可升级项**
- 未改动任何 UI 与播放逻辑，避免引入新问题

## 📦 版本号
- versionCode 575 / versionName 0.1.12
