# listenDTV v0.1.14 更新说明

## 🔧 修复：局域网同步搜不到设备（本次重点）

v0.1.13 加的「同步播放历史」在部分设备上互相搜不到对方，本次彻底排查并根治。

**根因**：扫描用的网络请求复用了全局客户端，`192.168.x.x` 这类局域网地址被当成域名去走 DoH 解析和代理规则——局域网 IP 根本不该做域名解析，更不该走代理，于是探测请求全部静默失败。

**修复内容**：

- **扫描改用直连专用客户端**：局域网探测不再走 DoH、不再走代理。
- **新增 UDP 广播发现**：打开同步弹窗后向局域网发一次广播，对方设备直接应答自己的信息——一次往返即可互认，不再靠猜网段和端口。对方没开应用时 TCP 扫描兜底，新旧版本互通不受影响。
- **扫描更全面**：遍历本机所在的全部网段（最多 3 个，防 VPN/热点干扰），同时探测对方实际监听端口和默认端口 9978——之前只扫一个网段、只试一个写死端口。
- **扫描过程可见**：弹窗内实时显示「正在扫描局域网设备…」「扫描完成，找到 N 台设备」；一无所获时给出「请确认两台设备都已打开本应用并连接同一 Wi-Fi」提示，不再是无声的空列表。
- **手动输入地址更宽容**：直接输 `192.168.1.7` 会自动补全 `http://` 和端口。

> 使用提示：两台设备都升级到本版效果最佳（都支持广播秒发现）；只升一台也能通过 TCP 扫描找到对方。

## ✅ 关联逻辑审计

- 同步链路 11 个文件与上游 FongMi/TV 逐字节一致，确认功能失灵不是 fork 改坏，而是上游设计缺陷，本次为最小增量修复。
- 播放器、播放逻辑、历史/收藏数据结构均未触碰。

## 📦 依赖

- 各关键库均处于最新稳定版，本轮无可升级项（维持 v0.1.12 结论）。

---

**versionCode**: 577　**versionName**: 0.1.14
