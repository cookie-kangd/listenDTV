# listenDTV v0.1.10 更新内容

## 🔊 收听模式改为「按视频记忆」
- 之前切换收听模式是全局生效：关掉一个视频的收听，其他视频也跟着变了
- 现在每部视频独立记住自己的收听开关（按站点+影片记录），互不影响
- 没有手动切换过的视频默认开启收听模式
- 记录条数设有上限保护，不会无限增长

## 📦 修复 OTA 升级下载到旧版本包的问题
- 有用户反馈从 v0.1.8 升级时，下载到的是 v0.1.8 的安装包而不是最新的
- 根因：下载走的 gh-proxy 加速链路存在中间缓存，新版本发布后代理仍返回旧的缓存包
- 本次修复三处：
  - 检查更新 / 下载安装包的所有请求都带上唯一时间戳参数，绕开中间缓存
  - 下载到本地的升级包文件名改为带版本号（如 update_0.1.10.apk），不同版本不再共用同一个文件
  - 启动和下载前会自动清理旧命名格式的升级包残留，杜绝旧包被误装
- 如仍遇到「提示已是最新但版本不对」的情况，请在系统设置里清除一次应用缓存后重试

## 🎬 播放逻辑说明
- 播放器错误处理保持 v0.1.9 恢复后的 v0.1.6 稳定逻辑，未做改动
- 播不了会正常报错并可手动重试，不再出现无限加载

## 🧹 其他
- 性能巡检：无新增风险项
- 依赖核查：与上一版一致，无变化
- 版本号 versionCode 573 / versionName 0.1.10
