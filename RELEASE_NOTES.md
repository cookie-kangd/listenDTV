# listenDTV v0.1.20 更新说明

## 🧹 精简：移除 WebDAV 云同步，同步入口收敛到播放历史页

### 1. 移除 WebDAV 云同步

实测未达预期，整套功能移除（云端按钮、配置弹窗、WebDAV 客户端、相关配置项与文案）：

- 删除 `WebDav` 客户端类与云端同步按钮、图标、12 条三语文案
- 删除 `Setting` 中 webdav_url/user/pass 三项持久化
- 同步弹窗保留：局域网自动发现（mDNS + UDP 广播 + TCP 扫描）、扫码绑定、四种模式（双向/上传/回传/拉取）

### 2. 设置页移除同步入口

- 设置页的「同步播放历史 / 同步收藏」卡片整块移除
- **唯一入口在：播放历史页右上角同步图标**（原有按钮，未改动位置）
- 收藏页暂无同步入口；如需要可在收藏页加同款按钮

versionCode 584 · versionName 0.1.20
