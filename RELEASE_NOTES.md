# listenDTV v0.1.19 更新说明

## 🐛 修复云端同步两个问题

### 1. 云端同步总是报「HTTP error」

根因：上传用的是 HTTP `POST`，而 WebDAV 上传必须是 `PUT`——坚果云对资源路径的 POST 一律返回 501，重试同样失败，于是弹出笼统的 "HTTP error"。已改为标准 `PUT`。

已用坚果云真实账号实测验证：PUT 上传、目录创建、下载回读全部正常（201/201/200）。

- 上传失败时错误提示现在会带上具体 HTTP 状态码（如 `HTTP 401`），不再是笼统的 "HTTP error"
- 认证失败（HTTP 401）会明确提示「请检查账号与应用密码」

### 2. 密码每次重新打开都被清空

根因：配置只在点「上传/下载」时才保存。如果填完直接按返回键/取消关闭弹窗，改动全部丢弃。现在**无论怎么关闭弹窗（上传/下载/取消/返回键）都会自动保存**三项配置。

versionCode 583 · versionName 0.1.19
