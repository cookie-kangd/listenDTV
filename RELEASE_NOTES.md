# listenDTV v0.1.13 更新内容

## 🎛 播放器底部按钮顺序调整
- 「选集」按钮从最左边挪到最右边——现在它是底部按钮栏里最靠右的那一个
- 功能完全不变，其余按钮顺序原样保留（解析 / 线路 / 解码 / 重置 / 重播 / 循环 / 倍速 / 画面比例，以及右侧的字幕 / 音轨 / 视轨 / 弹幕 / 版本 / 章节 / 片头 / 片尾）

## 📐 播放器中间按钮：真正居中 + 按宽度比例缩放
- **居中修正**：中间那排按钮原来既要被左右两条侧栏夹住、又要居中，两者互相冲突，结果落在了"侧栏之间那条带子"的中心而不是画面中心。现在改成相对画面真正居中，竖屏横屏播放键都精确对准中线
- **尺寸修正**：按钮尺寸原来写死 30dp / 38dp，手机竖屏看着正常，横屏和平板上就明显偏小。现在按屏幕宽度比例缩放（以 420dp 宽为基准，最多放大 1.7 倍）：
  - 竖屏约 360dp 宽 → 保持原有尺寸，观感不变
  - 横屏约 640dp 宽 → 放大约 1.5 倍，按钮不再显得空
  - 平板/超宽屏 → 封顶 1.7 倍，不会大到压住侧栏
- 旋转屏幕、进出全屏都会重新计算；尺寸始终以 XML 里的原始值为基准换算，反复旋转不会"越放越大"

## 🔄 设置新增「同步播放历史 / 同步收藏」
- 设置页新增一行两个入口：**同步播放历史**、**同步收藏**，点开就是设备列表
- 用法：两台设备连同一个 WiFi，各自打开 App → 在 A 上进入该页面 → 列表里会出现 B → 点一下 B，A 的播放历史就同步过去了（长按可强制覆盖对方全部历史）
- **原理与安全性**：走局域网直连，扫描到对方设备后把数据 POST 到对方 App 内置的 HTTP 服务上。不经过任何第三方服务器、不需要账号、不上传隐私、速度最快
- **合并规则**：对方按剧名合并，并且只在新记录的更新时间更晚时才写入，**不会把你看得更靠前的那台设备的进度覆盖掉**；同时会一并带上线路配置，对方还没配号源时会自动加载后再合并
- 这套通道其实早就在 App 里了（原来藏在「播放历史」页右上角菜单的同步图标里），本版把它提到设置页，顺手把「收藏」也一起暴露出来
- **跨网络**（比如公司电脑 ↔ 家里电视）局域网方案做不到，需要走 WebDAV / 网盘中转。本版没有做，需要的话可以单独加一版

## 📦 版本号
- versionCode 576 / versionName 0.1.13
