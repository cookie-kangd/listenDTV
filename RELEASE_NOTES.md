# listenDTV v0.1.7 更新内容

## 🎮 播放器控制（竖屏适配）
- 修复竖屏模式下播放控制按钮排列问题：中间控制栏改为宽度自适应、按钮大小自适应，无论屏幕多窄（含竖屏多出的按钮）都能完整显示「听播」「画中画」等全部按钮，不再被裁切

## 🔄 播放网络重试
- 播放途中因网络问题中断时自动重试（最多 3 次，每次间隔 1.5 秒），并在重试时提示「网络不稳定，正在重试」
- 多次重试仍失败则弹出「网络错误，请检查网络连接」提示，不再无声卡死

## 🎨 毛玻璃兼容
- 毛玻璃底栏增加低版本安卓兼容：在 Android 5.0 以下（不支持现代合成渲染）自动回退为更新前默认的透明底色，避免毛玻璃显示异常

## 📦 OTA 更新下载优化
- 修复低版本安卓更新下载/停止下载时报「permission defined」的问题：APK 缓存改为存放在应用私有目录（外部缓存，不可用时回退内部缓存），全程无需任何存储权限
- 停止下载 / 下载失败时自动删除已下载的缓存碎片文件，避免残留垃圾

## 📝 其他
- 版本号 versionCode 570 / versionName 0.1.7
