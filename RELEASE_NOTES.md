# listenDTV v0.1.15 更新说明

## 🔧 修复：局域网同步搜不到设备（三通道发现，承接 578 重建版）

上一版（577）的修复在部分真机上依然互搜不到，本次按"业界最佳方案"重做发现链路，从单通道升级为**三通道冗余发现**。（578 为同版本号重建版，应用内 OTA 无法识别，故顺延为 v0.1.15 正式发布。）

**上一版残留的三个根因**：

1. **UDP 广播收不到**：Android Wi-Fi 驱动默认丢弃"不是明确发给本机"的包，应用必须持有 `MulticastLock`（只有 Manifest 权限没用）。没有锁时，对方设备的广播应答器根本听不见广播——典型"同一 WiFi 就是搜不到"。
2. **对方设备可能压根没开服务**：内嵌 HTTP 服务只在"配置加载过"后才启动。刚装的 App 直接进设置开同步弹窗，两台设备都没监听端口，扫描自然两两落空。现在打开同步/投屏弹窗即自动拉起本机服务。
3. **TCP 扫描算力不够**：20 线程 × 1.2 秒超时 × 254 地址 × 2 端口 ≈ 30 秒，刚好卡在 30 秒截止线上，排在后面的地址永远扫不到。

**本次修复（mDNS 为主，三通道兜底）**：

- **新增 mDNS 发现（Chromecast / AirPlay 同款方案）**：用系统自带 NSD 栈注册并浏览 `_listendtv._tcp` 服务。多播收发由系统 mdnsd 完成，不受厂商 Wi-Fi 过滤影响；服务通告自带真实端口，不用猜 9978-9998；对整个局域网段生效，不再受 /24 限制。**两台设备都升到本版后，mDNS 通道通常 2 秒内即可互认。**
- **广播应答器补上 MulticastLock**：UDP 广播通道恢复工作。
- **扫描专用线程池 20 → 48 线程**、单次探测超时 1.2s → 0.8s：全量 TCP 扫描从"贴着 30 秒截止线"缩到 10 秒内完成，不再有地址被截止时间截胡。
- **扫描三通道冗余**：mDNS → UDP 广播 → TCP 扫描，任一通道被路由器/AP 隔离挡住，其余通道仍可工作。

> 使用提示：**两台设备都升级到本版**。若仍搜不到，基本可断定路由器开启了"AP 隔离 / 访客网络 / 无线隔离"——请在路由器后台关闭该选项，或两台设备改连手机热点验证。

## ✅ 关联逻辑审计

- 发现链路逐层核查：Server 启动时机、Device JSON 端点、OkHttp 直连客户端（Dns.SYSTEM + NO_PROXY，上一版已修对）、self 过滤、端口推断——均已确认或修复。
- 播放器、播放逻辑、历史/收藏数据结构均未触碰。

## 📦 依赖

- 各关键库均处于最新稳定版（okhttp 5.5.0 / media3 1.11.0 / room 2.8.4 等），本轮无可升级项（维持 v0.1.12 结论）。

---

**versionCode**: 579　**versionName**: 0.1.15
