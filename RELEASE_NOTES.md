# listenDTV v0.1.8 更新内容

## 🧹 修复退出清理缓存不彻底
- 根因修复：设置里显示的「缓存」大小统计的是应用完整缓存（内部 + 外部目录，含全部子目录），而旧清理逻辑只清其中一个目录且不递归，所以退出后再进来始终残留约 1.6MB
- 现在「退出时清理缓存」改为递归深度清理内部 + 外部两个缓存根目录，退出 / 启动时均会执行，缓存可以真正归零
- 设置页手动点击「缓存」清理也改用同一深度清理逻辑，点完立即看到大小下降

## 🔄 新增「自动检查更新」开关
- 设置页新增「自动检查更新」选项，默认开启：开启时打开 App 自动检查有没有最新版本；关闭后打开 App 不再自动检查
- 手动检查（设置-关于）不受该开关影响，随时可以手动检查

## 🎮 播放控制按钮比例优化
- 修复竖屏模式按钮过大：中间控制栏改为固定比例的精致尺寸（播放键稍大突出层级），横竖屏观感一致协调
- 修复竖屏模式少按钮显示：中间控制栏现在严格约束在左侧弹幕按钮与右侧面板之间，不再被两侧遮挡，「听播」「画中画」「播放」「上下曲」「快进快退」全部按钮完整可见
- 左侧弹幕按钮与顶部图标同步微调，间距更均匀

## 📦 其他优化
- 缓存清理与 OTA 下载缓存目录统一收纳在应用私有缓存内，存储占用更可控
- 依赖核查：核心依赖均为当前最新稳定版，无影响功能的安全升级，保持稳定性优先
- 版本号 versionCode 571 / versionName 0.1.8
