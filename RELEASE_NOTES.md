# listenDTV v0.1.17 更新说明

## 🆕 云端同步（WebDAV）—— 模拟器/跨网络同步的最终方案

上一版的"拉取模式"有一个隐蔽 bug：同步模式被设置项钳制在 0–2，第 4 档"拉取"存进去就被改写成"回传"，导致模拟器场景仍然不通。本版修复该钳制，并新增**云端同步**，同时保留全部局域网功能（自动搜索、手输 IP、二维码、拉取模式现在也真正可用了）。

## 为什么选 WebDAV（而不是账号系统）

账号登录同步需要自建用户系统 + 常驻云端后端 + 长期运维，个人项目不现实。WebDAV 只需一个网盘账号：**坚果云**免费支持 WebDAV（国内访问快），两台设备填同一个账号即可互相同步，**无论什么网络拓扑都能通**——模拟器 NAT、不同 WiFi、甚至异地。

## 用法（手机 + 模拟器推荐流程）

1. 两台设备都升级到 0.1.17。
2. 坚果云官网注册账号 → 账户信息 → 安全选项 → 添加"应用密码"（WebDAV 用这个密码，不是登录密码）。
3. 两台设备：同步弹窗 → 云端图标 → 填入：
   - 地址：`https://dav.jianguoyun.com/dav/`
   - 账号：坚果云登录邮箱
   - 密码：应用密码
4. 数据多的一台点**上传**，另一台点**下载** → 历史自动合并到本机。
5. 同步收藏：切换弹窗顶部的历史/收藏入口后重复上传/下载。

## 修复

- **同步模式钳制 bug**：`MAX_SYNC_MODE` 2→3，v0.1.16 的"拉取"模式（第 4 档图标）现在真正生效。此前选它会静默变回"回传"，NAT 环境下必然失败。
- 云端下载与拉取模式的导入改走 `127.0.0.1` 环回，不再依赖本机 IP 选取是否正确。
- 云端上传首次使用会自动在网盘创建 `listendtv` 目录。

## 保留不变

- 局域网自动搜索（mDNS + UDP 广播 + TCP 扫描）：两台真机同一 WiFi 仍可互搜互同步。
- 手动输入 IP、我的二维码、4 档同步模式（双向/上传/回传/拉取）全部保留。

versionCode 581，versionName 0.1.17。
